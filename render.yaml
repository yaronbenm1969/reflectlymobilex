services:
  - type: web
    name: reflectly-server
    runtime: node
    buildCommand: cd server && npm install --legacy-peer-deps
    startCommand: cd server && node video-converter-api.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
